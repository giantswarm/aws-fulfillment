apiVersion: apps/v1
kind: Deployment
metadata:
  name: aws-fulfillment
  namespace: aws-fulfillment
  labels:
    app: aws-fulfillment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: aws-fulfillment
  template:
    metadata:
      labels:
        app: aws-fulfillment
    spec:
      containers:
      - name: aws-fulfillment
        image: gsoci.azurecr.io/giantswarm/aws-fulfillment:0.0.0-8901fea0bb8c952ce50cb447d0e4b1c78d830dd3
        resources:
          requests:
            memory: "100Mi"
            cpu: "50m"
          limits:
            memory: "100Mi"
            cpu: "50m"
        env:
        - name: AWS_ACCESS_KEY_ID
          valueFrom:
            secretKeyRef:
              name: aws-fulfillment
              key: AWS_ACCESS_KEY_ID
              optional: false
        - name: AWS_SECRET_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: aws-fulfillment
              key: AWS_SECRET_ACCESS_KEY
              optional: false
        - name: SLACK_TOKEN
          valueFrom:
            secretKeyRef:
              name: aws-fulfillment
              key: SLACK_TOKEN
              optional: false
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
          runAsNonRoot: true
          seccompProfile:
            type: RuntimeDefault
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchLabels:
                  app: aws-fulfillment
              topologyKey: "kubernetes.io/hostname"
